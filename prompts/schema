{
  "one_liner": ["the command broken up as an array, e.g., ['npx','@example/example-program']"],
  "requires": ["required environment variables exactly as documented, e.g., 'EXAMPLE_API_KEY'. Leave empty array if none are required."],
  "status": "ok | fail"
}
